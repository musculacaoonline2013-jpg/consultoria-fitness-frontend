/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configurações básicas (como o seu projeto é simples, deixe o mínimo)
  // Caso você use TailwindCSS ou outras configurações, elas virão aqui.
};

module.exports = nextConfig;