'use client';

import React from 'react';

type Step4HealthProps = {
  [key: string]: any;
};

const Step4Health: React.FC<Step4HealthProps> = (props) => {
  return (
    <div>
      {/* Passo 4 - Saúde / restrições */}
      <p>Formulário - Passo 4 (Saúde)</p>
    </div>
  );
};

export default Step4Health;
